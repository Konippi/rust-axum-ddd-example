#!/bin/sh -eu

psql -v ON_ERROR_STOP=1 -U "${POSTGRES_USER}" <<- _EOT_
    BEGIN;
        CREATE USER ${POSTGRES_APP_USER} WITH PASSWORD '${POSTGRES_APP_PASSWORD}';
        ALTER ROLE ${POSTGRES_DB} WITH CREATEDB CREATEROLE LOGIN;
    COMMIT;
_EOT_
